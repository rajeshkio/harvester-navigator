// Simple HTTP data fetching (replaces WebSocket)
const WebSocketManager = {
    connect() {
        // No longer needed - app.js handles the HTTP fetching
        console.log('WebSocket replaced with HTTP fetching');
    }
};
